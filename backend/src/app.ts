import cors from 'cors';
import dotenv from 'dotenv';
import mongoose from 'mongoose';

dotenv.config()

// mongoose
//     .connect('mongodb://127.0.0.1:27017/va-doostan')
//     .then(() => {
//         app.listen(5000)
//     }).catch((err) => {
//         console.log('connection failed', err);
// })
